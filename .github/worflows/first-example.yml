name: My First Workflow

# Trigger
on: push

# jobs
jobs:
  first_job:
      runs-on: ubuntu-latest
      steps:
        - name: welcome message
           run: echo "My first Github Actions Job"

        - name: List files
          run: ls

        - name: Reade file
          run: cat README.md
         